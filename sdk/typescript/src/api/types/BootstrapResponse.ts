// This file was auto-generated by Fern from our API Definition.

export interface BootstrapResponse {
    agent: BootstrapResponse.Agent;
    identity: BootstrapResponse.Identity;
    organization: BootstrapResponse.Organization;
    pod: BootstrapResponse.Pod;
    inbox: BootstrapResponse.Inbox;
    capabilities: BootstrapResponse.Capabilities;
    limits: BootstrapResponse.Limits;
    links: BootstrapResponse.Links;
}

export namespace BootstrapResponse {
    export interface Agent {
        id: string;
        trustTier: Agent.TrustTier;
    }

    export namespace Agent {
        export const TrustTier = {
            Bootstrap: "bootstrap",
            Verified: "verified",
            Established: "established",
        } as const;
        export type TrustTier = (typeof TrustTier)[keyof typeof TrustTier];
    }

    export interface Identity {
        id: string;
        handle: string;
        emailAddress: string;
    }

    export interface Organization {
        id: string;
    }

    export interface Pod {
        id: string;
        name: string;
    }

    export interface Inbox {
        id: string;
        address: string;
        status: Inbox.Status;
    }

    export namespace Inbox {
        export const Status = {
            Active: "active",
        } as const;
        export type Status = (typeof Status)[keyof typeof Status];
    }

    export interface Capabilities {
        receive: boolean;
        send: boolean;
        imap: boolean;
        smtp: boolean;
        customDomains: boolean;
        a2a: boolean;
    }

    export interface Limits {
        messagesPerDay: number;
        recipientsPerMessage: number;
        attachmentBytes: number;
    }

    export interface Links {
        inbox: string;
        messages: string;
        events: string;
        imap: string;
        smtp: string;
        a2a: string;
        agentCard: string;
    }
}
