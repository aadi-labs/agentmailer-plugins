// This file was auto-generated by Fern from our API Definition.

export interface ChannelEndpoint {
    id: string;
    inboxId: string;
    podId: string;
    channel: ChannelEndpoint.Channel;
    address: string;
    status: ChannelEndpoint.Status;
    capabilities: ChannelEndpoint.Capabilities.Item[];
    createdAt: string;
    updatedAt: string;
}

export namespace ChannelEndpoint {
    export const Channel = {
            Sms: "sms",
            Mms: "mms",
            Rcs: "rcs",
            Whatsapp: "whatsapp",
            Voice: "voice"
        } as const;
    export type Channel = typeof Channel[keyof typeof Channel];
    export const Status = {
            Provisioning: "provisioning",
            Active: "active",
            Disabled: "disabled",
            Failed: "failed"
        } as const;
    export type Status = typeof Status[keyof typeof Status];
    export type Capabilities = Capabilities.Item[];

    export namespace Capabilities {
        export const Item = {
                Sms: "sms",
                Mms: "mms",
                Rcs: "rcs",
                Whatsapp: "whatsapp",
                Voice: "voice"
            } as const;
        export type Item = typeof Item[keyof typeof Item];
    }
}
