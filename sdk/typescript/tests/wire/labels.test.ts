// This file was auto-generated by Fern from our API Definition.

import { AgentMailerClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("LabelsClient", () => {
    test("list", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentMailerClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {
            labels: [{ podId: "podId", name: "name", color: "color", id: "id", createdAt: "2024-01-15T09:30:00Z" }],
        };

        server.mockEndpoint().get("/v1/labels").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.labels.list();
        expect(response).toEqual(rawResponseBody);
    });

    test("create", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentMailerClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });
        const rawRequestBody = { podId: "podId", name: "name" };
        const rawResponseBody = {
            label: { podId: "podId", name: "name", color: "color", id: "id", createdAt: "2024-01-15T09:30:00Z" },
        };

        server
            .mockEndpoint()
            .post("/v1/labels")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.labels.create({
            podId: "podId",
            name: "name",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("delete", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentMailerClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        server.mockEndpoint().delete("/v1/labels/labelId").respondWith().statusCode(200).build();

        const response = await client.labels.delete("labelId");
        expect(response).toEqual(undefined);
    });

    test("updateMessage", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentMailerClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });
        const rawRequestBody = {};
        const rawResponseBody = { labels: ["labels"] };

        server
            .mockEndpoint()
            .patch("/v1/messages/messageId/labels")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.labels.updateMessage("messageId", {});
        expect(response).toEqual(rawResponseBody);
    });

    test("updateThread", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentMailerClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });
        const rawRequestBody = {};
        const rawResponseBody = { labels: ["labels"] };

        server
            .mockEndpoint()
            .patch("/v1/threads/threadId/labels")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.labels.updateThread("threadId", {});
        expect(response).toEqual(rawResponseBody);
    });
});
