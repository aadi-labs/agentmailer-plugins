// This file was auto-generated by Fern from our API Definition.

import { AgentMailerClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("EventsClient", () => {
    test("createTicket", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentMailerClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });
        const rawRequestBody = { podId: "podId" };
        const rawResponseBody = { ticket: "ticket", expiresIn: 1, websocketUrl: "websocketUrl" };

        server
            .mockEndpoint()
            .post("/v1/events/tickets")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.events.createTicket({
            podId: "podId",
        });
        expect(response).toEqual(rawResponseBody);
    });
});
