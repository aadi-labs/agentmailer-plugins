// This file was auto-generated by Fern from our API Definition.

import { AgentMailerClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("InboxesClient", () => {
    test("list", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentMailerClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {
            inboxes: [
                {
                    id: "id",
                    podId: "podId",
                    address: "address",
                    displayName: "displayName",
                    clientId: "clientId",
                    metadata: { key: "value" },
                    status: "provisioning",
                    createdAt: "2024-01-15T09:30:00Z",
                    updatedAt: "2024-01-15T09:30:00Z",
                },
            ],
            nextPageToken: "nextPageToken",
        };

        server.mockEndpoint().get("/v1/inboxes").respondWith().statusCode(200).jsonBody(rawResponseBody).build();

        const response = await client.inboxes.list();
        expect(response).toEqual(rawResponseBody);
    });

    test("create", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentMailerClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });
        const rawRequestBody = {};
        const rawResponseBody = {
            inbox: {
                id: "id",
                podId: "podId",
                address: "address",
                displayName: "displayName",
                clientId: "clientId",
                metadata: { key: "value" },
                status: "provisioning",
                createdAt: "2024-01-15T09:30:00Z",
                updatedAt: "2024-01-15T09:30:00Z",
            },
        };

        server
            .mockEndpoint()
            .post("/v1/inboxes")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.inboxes.create();
        expect(response).toEqual(rawResponseBody);
    });

    test("get", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentMailerClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {
            inbox: {
                id: "id",
                podId: "podId",
                address: "address",
                displayName: "displayName",
                clientId: "clientId",
                metadata: { key: "value" },
                status: "provisioning",
                createdAt: "2024-01-15T09:30:00Z",
                updatedAt: "2024-01-15T09:30:00Z",
            },
        };

        server
            .mockEndpoint()
            .get("/v1/inboxes/inboxId")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.inboxes.get("inboxId");
        expect(response).toEqual(rawResponseBody);
    });

    test("delete", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentMailerClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        server.mockEndpoint().delete("/v1/inboxes/inboxId").respondWith().statusCode(200).build();

        const response = await client.inboxes.delete("inboxId");
        expect(response).toEqual(undefined);
    });

    test("update", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentMailerClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });
        const rawRequestBody = {};
        const rawResponseBody = {
            inbox: {
                id: "id",
                podId: "podId",
                address: "address",
                displayName: "displayName",
                clientId: "clientId",
                metadata: { key: "value" },
                status: "provisioning",
                createdAt: "2024-01-15T09:30:00Z",
                updatedAt: "2024-01-15T09:30:00Z",
            },
        };

        server
            .mockEndpoint()
            .patch("/v1/inboxes/inboxId")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.inboxes.update("inboxId");
        expect(response).toEqual(rawResponseBody);
    });

    test("issueCredentials", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentMailerClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {
            username: "username",
            password: "password",
            imap: { host: "host", port: 1, security: "tls" },
            smtp: { host: "host", port: 1, security: "tls" },
        };

        server
            .mockEndpoint()
            .post("/v1/inboxes/inboxId/credentials")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.inboxes.issueCredentials("inboxId");
        expect(response).toEqual(rawResponseBody);
    });
});
