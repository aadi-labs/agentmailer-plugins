// This file was auto-generated by Fern from our API Definition.

import { AgentMailerClient } from "../../src/Client";
import { mockServerPool } from "../mock-server/MockServerPool";

describe("A2AClient", () => {
    test("sendTask", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentMailerClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });
        const rawRequestBody = { jsonrpc: "2.0", method: "method" };
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .post("/a2a/handle")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.a2A.sendTask("handle", {
            jsonrpc: "2.0",
            method: "method",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("getAgentCard", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentMailerClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });

        const rawResponseBody = {
            name: "name",
            description: "description",
            supportedInterfaces: [{ url: "url", protocolBinding: "JSONRPC", protocolVersion: "1.0" }],
            provider: { url: "url", organization: "organization" },
            version: "version",
            documentationUrl: "documentationUrl",
            capabilities: { streaming: true, pushNotifications: true, extendedAgentCard: true },
            securitySchemes: { key: "value" },
            securityRequirements: [{ key: ["value"] }],
            defaultInputModes: ["defaultInputModes"],
            defaultOutputModes: ["defaultOutputModes"],
            skills: [
                {
                    id: "id",
                    name: "name",
                    description: "description",
                    tags: ["tags"],
                    examples: ["examples"],
                    inputModes: ["inputModes"],
                    outputModes: ["outputModes"],
                },
            ],
        };

        server
            .mockEndpoint()
            .get("/a2a/handle/.well-known/agent-card.json")
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.a2A.getAgentCard("handle");
        expect(response).toEqual(rawResponseBody);
    });

    test("updateTask", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentMailerClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });
        const rawRequestBody = { state: "TASK_STATE_SUBMITTED" };
        const rawResponseBody = {
            task: {
                id: "id",
                contextId: "contextId",
                status: {
                    state: "TASK_STATE_SUBMITTED",
                    timestamp: "2024-01-15T09:30:00Z",
                    message: { messageId: "messageId", role: "ROLE_USER", parts: [{}] },
                },
                artifacts: [{ artifactId: "artifactId", parts: [{}] }],
                history: [{ messageId: "messageId", role: "ROLE_USER", parts: [{}] }],
                metadata: { key: "value" },
            },
        };

        server
            .mockEndpoint()
            .post("/v1/a2a/tasks/taskId")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.a2A.updateTask("taskId", {
            state: "TASK_STATE_SUBMITTED",
        });
        expect(response).toEqual(rawResponseBody);
    });

    test("updateIdentity", async () => {
        const server = mockServerPool.createServer();
        const client = new AgentMailerClient({ maxRetries: 0, apiKey: "test", environment: server.baseUrl });
        const rawRequestBody = {};
        const rawResponseBody = { key: "value" };

        server
            .mockEndpoint()
            .patch("/v1/a2a/identity")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.a2A.updateIdentity();
        expect(response).toEqual(rawResponseBody);
    });
});
